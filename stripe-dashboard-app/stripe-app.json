{
  "$schema": "https://stripe.com/stripe-app.schema.json",
  "id": "com.recoverflow.dashboard",
  "name": "RecoverFlow",
  "version": "0.0.5",
  "icon": "./icon.png",
  "ui_extension": {
    "views": [
      {
        "viewport": "stripe.dashboard.customer.detail",
        "component": "CustomerDetail"
      },
      {
        "viewport": "stripe.dashboard.invoice.detail",
        "component": "InvoiceDetail"
      },
      {
        "viewport": "stripe.dashboard.drawer.default",
        "component": "AppOverview"
      },
      {
        "viewport": "settings",
        "component": "AppSettings"
      }
    ],
    "content_security_policy": {
      "connect-src": [
        "https://api.recoverflow.org/app/"
      ],
      "purpose": "The app loads your RecoverFlow recovery data from the RecoverFlow API."
    }
  },
  "extensions": null,
  "permissions": [
    {
      "permission": "customer_read",
      "purpose": "Identifies the customer you are viewing so the app can show that customer's RecoverFlow recovery cases and dunning email progress."
    },
    {
      "permission": "invoice_read",
      "purpose": "Identifies the invoice you are viewing so the app can show whether it has a RecoverFlow recovery case and how far it has progressed."
    },
    {
      "permission": "invoice_write",
      "purpose": "Retries an unpaid invoice at the moment RecoverFlow judges a retry can succeed. RecoverFlow only ever pays an invoice you already issued; it never creates invoices or charges of its own."
    },
    {
      "permission": "event_read",
      "purpose": "Receives the failed-payment and paid notices Stripe sends for your invoices, so a recovery case opens the moment a payment fails and closes the moment it is paid."
    },
    {
      "permission": "charge_read",
      "purpose": "Sees when a payment RecoverFlow recovered is later refunded, so the amount handed back is taken off the recovery and never billed."
    },
    {
      "permission": "dispute_read",
      "purpose": "Sees when a payment RecoverFlow recovered is later disputed, so the amount handed back is taken off the recovery and never billed."
    }
  ],
  "post_install_action": {
    "type": "external",
    "url": "https://recoverflow.org/"
  },
  "constants": {
    "API_BASE": "https://api.recoverflow.org/app/v1"
  },
  "allowed_redirect_uris": [
    "https://api.recoverflow.org/connect/stripe/callback"
  ],
  "distribution_type": "public",
  "stripe_api_access_type": "oauth",
  "sandbox_install_compatible": true
}