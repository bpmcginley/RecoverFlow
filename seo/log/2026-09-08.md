# 2026-09-08

Seventh run against `seo/ROUTINE.md`. The previous log is `2026-09-07.md`, which
lives only on the branch behind PR #6 and is not on `main` yet.

Nothing landed broken. One pull request is open from last week, so the three
pages it edits were treated as frozen. No fresh Search Console file, which means
the four prose changes shipped since 6 September are all still unmeasurable.

## Answering last run's list

**1. PR #6 has not merged.** Open since 7 September. It edits
`/blog/stripe-generic-decline-code/`, `/blog/do-not-honor-stripe-decline/` and
`/blog/stripe-incorrect-cvc-decline/`, and none of the three was touched here.
No fresh `seo/gsc/` file exists, so the first outcome this routine can measure,
whether PR #3 closed the `card_velocity_exceeded` split, is still not readable.

**2. Pulled before Step 1.** `git fetch` then `git pull --ff-only`, both no-ops:
local `main` was already at `879ab2b`. Step 1 ran against the current tree.

`git status` again reported two files modified on a clean checkout,
`docs/blog/do-not-honor-stripe-decline/index.html` and `docs/blog/index.html`.
Both byte identical to `HEAD`, 24832 and 36407 bytes either side. Same stat or
line ending artifact this log has now declined five times. Discarded.

**3. Ran `report_gsc.py` first.** Page dimension printed first. No new page
appeared in the striking band and no row anywhere changed, because it is the
same file.

**4. Did not rewrite a title against dark impressions.** The one title changed
here is a truncation fix on a page with a named query. See below.

**5. Warning count was 11 at the start, as expected, and is 10 now.** It is the
same 10 either way once PR #6 merges: that PR takes `incorrect_cvc` off the list
and this run takes `processing_error` off it, so the two are independent and the
count lands at 9 when both are on `main`.

`validate_site.py` reports no problems. `audit_page_sources.py` reports 61 pages,
37 generated and 24 hand written, with no page moving when the builders ran.

## What Search Console showed

`seo/gsc/2026-09-01.json`, now 7 days old and the same file the last five runs
read. 718 impressions and 2 clicks across 150 named queries for 2 to 29 August,
against 2,938 impressions on the page dimension, of which 2,199 (75%) carry no
query. Unchanged, so the tables are not repeated.

The window still ends 29 August. PR #3, #4 and #5 merged on 6 September and PR #6
has not merged at all. Four prose changes are now waiting on a file that does not
exist, up from three last week. This is the same blocked measurement the last log
recorded, one change larger.

## Changed

1. **`/blog/stripe-processing-error-decline/` title cut from 63 characters to
   56.** "processing_error on Stripe: the decline you should simply retry" became
   "processing_error on Stripe: the decline you should retry". This is the
   candidate the last log queued by name. The page sits in the striking band on
   its own code as a query, `processing_error` at 3 impressions and position 10.3
   with no clicks, and the title truncates at any position, so the defect is
   known without knowing the query text. The word dropped is "simply"; the query
   term, the brand, "decline" and "retry" all survive. Hand written page, so all
   four slots that carry the string moved together, `<title>`, `og:title`, the
   JSON-LD headline and the `h1`. The description was already 151 characters and
   does not contain the phrase, so it is unchanged. The blog hub picked up the
   new heading and JSON-LD headline on rebuild. `docs/llms.txt` carries the title
   too and is static, so it was edited by hand.

2. **`/blog/stripe-decline-codes-that-stop-retries/` gains one `related` link to
   `/blog/stripe-authentication-required-recovery/`.** The hub names nine hard
   codes and, before PR #5, linked to the guide for none of them. Of the four
   still unlinked, `authentication_required` is the only one with a named query
   at all: 4 impressions at position 11.2 with no clicks, in the band the routine
   calls the highest return target on the site. There is also a split of the same
   shape that justified PR #5, on `card_error authentication_required`, where the
   guide ranks 37.7 and takes 3 of 6 impressions while `/tools/decline-code-lookup/`
   ranks 79.0 and takes the other 3. The hub mentions the code three times in its
   own prose and linked to its guide zero times. The guide already links back to
   the hub, so this makes the pair reciprocal. Generated page, so the link went
   into `related` in `build_content_pages.py`, and the comment there now records
   which three codes are still waiting and why.

Both are published prose, so both went to a branch and a pull request per Step 6.
Nothing went to `main` this week. The two changes are on different pages, so each
stays readable against the next Search Console file on its own.

## Considered and rejected

- **Adding an inbound link to `/blog/stripe-processing-error-decline/` as well.**
  It has 2 inbound links, the site minimum, and is a fair link target. It is also
  the page whose title changed this week, and changing two things on one page
  before either can be read makes neither readable. Take the link in a later run
  if the title alone does not move it.
- **Anything on `/blog/stripe-generic-decline-code/`,
  `/blog/do-not-honor-stripe-decline/` or `/blog/stripe-incorrect-cvc-decline/`.**
  All three are edited by PR #6, which is open. Frozen.
- **Anything on `/blog/visa-excessive-reattempts-rule/`.** PR #3 rewrote its
  title and description on 6 September and no file can read the result yet. Its
  query `previously_declined_do_not_retry`, 5 impressions at position 8.4 with no
  clicks, is a genuine striking band row and a fair future candidate, but not on
  top of an unmeasured change.
- **Acting on the `card_velocity_exceeded` split.** Unchanged for four runs. PR
  #3's title rewrite is still the cleanest test of it and still unreadable.
- **Making `/tools/decline-code-lookup/` link out to the guides it outdraws.**
  It already links to all of them, once each, including every page in every
  flagged split. Checked directly rather than assumed. The splits where the
  lookup tool takes impressions off a better ranked guide are therefore not a
  missing link problem, and there is nothing cheap left to do about them from
  inside `docs/`. Do not re-propose this as a linking fix.
- **A title rewrite on `/blog/stripe-subscription-past-due-vs-unpaid/`,
  `/blog/stripe-automatic-card-updates/`, `/blog/stripe-failed-payment-webhooks/`
  or `/blog/stripe-free-dunning-settings-checklist/`.** All four are dark, three
  of them completely. Constraint unchanged and recorded for a fourth time. The
  page level export in "Needs Bruce" 3 is the only thing that lifts it.
- **The remaining eight truncating titles.** None has a query at a position where
  the click is in reach. `/blog/stripe-call-issuer-decline/` at 63 characters is
  the shortest of them and has no query at all.
- **Hub links for `incorrect_number`, `lost_card`/`stolen_card` and
  `pickup_card`.** Still no query behind any of the three. The comment in
  `build_content_pages.py` now says so explicitly rather than listing four codes
  it no longer describes.
- **The `the bank returned the decline code do_not_honor` and
  `do_not_honor decline code` splits.** Both genuinely mis-targeted, both with
  every page ranking between 42 and 96. Nothing there is close enough for a link
  or a title to convert, and the do_not_honor page is frozen behind PR #6 anyway.
- **A new article.** No supporting query evidence, seven runs running.
- **The commercial head terms.** `decline recovery` 82.1, `failed payment
  recovery` 95.0, `recover failed payments` 95.7. Unchanged and out of reach.
- **A fresh fetch.** Still a decision rather than work, and still Bruce's.
- **The line ending noise in `git status`.** Declined a fifth time. Do not
  re-propose.

## Needs Bruce

1. **The cadence question is now blocking four changes, not three.** Nothing is
   scheduled, so the next file arrives only if someone runs the fetch. The
   default 28 day fetch on or after 29 September covers roughly 1 to 28 September
   and mostly postdates the 6 September merges, which is usable.
   `python3 scripts/fetch_gsc.py fetch --days 7` any time now gives a cleaner
   read sooner and would be the first file whose whole window postdates them.
   Either works. The current state does not.
2. **Two pull requests are open.** PR #6 from 7 September, one title cut and two
   links. This run's PR, one title cut and one link. Both small.
3. **Search Console's page level query export would unlock the biggest page on
   the site.** Filtering Performance to
   `/blog/stripe-subscription-past-due-vs-unpaid/` and exporting queries
   sometimes surfaces rows the site wide API call withholds. Its 373 dark
   impressions become writable if it works. `fetch_gsc.py import-csv` already
   accepts the export. Unchanged for three runs.
4. **`app.recoverflow.org` is still in the index**, 3 impressions on the brand
   query at position 10 against the marketing site's 7.1. Outside `docs/`, so
   this routine cannot reach it. Needs a `noindex` header or a robots rule on
   that deployment. Unchanged for four runs.

## Next run, in order

1. Whether PR #6 and this run's pull request merged, and whether a fresh
   `seo/gsc/` file exists. If one does and its window starts after 6 September,
   read the `card_velocity_exceeded` split first: it is the oldest blocked
   measurement and the site's best positioned cluster.
2. Pull before Step 1, then run `python3 scripts/report_gsc.py` before reading
   anything else.
3. Do not rewrite a title against dark impressions unless the page level export
   in "Needs Bruce" 3 has been run.
4. Warning count is 10 on this branch and 9 once PR #6 also merges. If it rises,
   a page shipped with a title that will not fit.
5. If the queue of unmeasured prose reaches five, stop shipping prose and say so.
   A routine that keeps writing against a window that ended 29 August is guessing
   with increasing confidence and no feedback.
