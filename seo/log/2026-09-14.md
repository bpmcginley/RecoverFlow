# 2026-09-14

Twelfth run against `seo/ROUTINE.md`. Previous log is `2026-09-13.md`. Scheduled run, now
weekly (`0 10 * * 1`), and today is the first Monday since that fix landed.

## What changed

1. **Fixed `scripts/report_gsc.py` picking the wrong file as "newest."** Step 2 failed
   outright: `KeyError: 'fetched'`. Cause: the file selector was `sorted(glob.glob("*.json"))`
   over all of `seo/gsc/`, and the page-filtered export the last run committed,
   `page-stripe-subscription-past-due-vs-unpaid-2026-08-02_2026-08-29.json`, sorts after
   `2026-09-01.json` alphabetically ('p' > '2'), so `files[-1]` picked it instead of the
   real newest dated file. That export has no `fetched` key, hence the crash. `seo/gsc/README.md`
   already says pulls land as `<date>.json`; the fix restricts the auto-detect glob to that
   pattern (`[0-9][0-9][0-9][0-9]-[0-9][0-9]-[0-9][0-9].json`) so a one-off named file can still
   be read by passing it explicitly as an argument but is never picked as "newest" by accident.
   Verified: `report_gsc.py` now reads `seo/gsc/2026-09-01.json` and prints the full report.
   Maintenance fix, committed straight to `main` per Step 6.

## What Step 2 showed

Same file as every run since 2026-09-01: `2026-09-01.json`, window 2026-08-02 to 2026-08-29,
13 days old (not stale). No fresher fetch exists yet; the last three logs worked out why
running `fetch_gsc.py fetch --days 7` before 2026-09-16 gives a window that still straddles
the 6 September merges, so it was not run today either. Today is 2026-09-14, still short of
that date. Nothing in the report is new: same 401-impression dark page
(`/blog/stripe-subscription-past-due-vs-unpaid/`), same `ai10325` SKU-collision term, same
`card_velocity_exceeded` split between the guide (pos 19.5-25.9) and `/tools/decline-code-lookup/`
(pos 43.4), same `past_due` bare term at position 21.3. All previously read and left alone;
re-reading the same window did not change any of those calls.

## Considered and rejected

- **Any prose change today.** The five-change ceiling from Step 3 was hit by the run before
  last: `git log --since=2026-08-29 -- docs/` shows five prose commits (ae8cd64, 68ade81,
  ccb3bce, 716a98d, 17df2e0) since the current GSC window's end date, all merged. No fresher
  `seo/gsc/*.json` has landed since, so the window that ceiling is measured against has not
  moved. Ran Steps 0, 1, 2 and 7 only, per the routine's own instruction for this state.
- **Acting on `card_velocity_exceeded` or `past_due` today.** Both are exactly what the last
  log queued for "once a fresher file lands." No fresher file landed. Left for the run on or
  after 2026-09-16.
- **Running `fetch_gsc.py fetch --days 7` today anyway.** Same reasoning as the last three
  logs: today is 2026-09-14, and the straddle with the 6 September merges only clears on
  2026-09-16. Waiting three more days costs nothing; a muddled file would become the input
  every future run reads.

## Needs Bruce

1. **`app.recoverflow.org` is still indexed**, unchanged from the last four logs. Outside
   `docs/`, so this routine cannot reach it.

Nothing else outstanding. The 2026-09-13 "confirm whether to run the fetch early" question is
answered by this run choosing to wait, consistent with that log's own recommendation.

## Next run, in order

1. If today is 2026-09-16 or later and no fresher file exists yet, run
   `python3 scripts/fetch_gsc.py fetch --days 7`.
2. Once that file lands: re-read the `card_velocity_exceeded` split (guide vs. the lookup
   tool) first, then reconsider whether `past_due` on
   `/blog/stripe-subscription-past-due-vs-unpaid/` (position 21.3, 93% dark) is worth a title
   change now that a real position number exists for the bare term.
3. Title-length backlog is still 9. Take one only when a query says the page is close enough
   for the click to matter, same rule as every prior run.
