# 2026-09-07

Sixth run against `seo/ROUTINE.md`. The previous log is `2026-09-06.md`.

The backlog cleared. PR #3, #4 and #5 all merged on 6 September, within about
ten minutes of each other. Four runs' worth of prose is now published, and this
run was the first since 31 August that started with nothing frozen.

## Answering last run's list

**1. All three merged.** #3 at 15:45 UTC, #4 at 15:47, #5 at 15:56, all on
6 September. Local `main` was three commits behind at the start of this run, so
the first health check ran against the pre-merge tree and reported the stale
warning count of 12. Fast-forwarded to `ccb3bce` and re-ran everything. Worth
knowing for future runs: Step 1 on a stale checkout measures the wrong tree.

`git status` also reported `docs/blog/stripe-decline-codes-that-stop-retries/`
as modified on what should have been a clean checkout. It was byte identical to
`HEAD`, 28023 bytes either side, a stat or line ending artifact and not an edit.
Discarded rather than committed. Same shape as the 37 file line ending noise
this log has declined three times.

**2. Ran `report_gsc.py` before reading anything.** Page dimension first, as it
now prints. No new page appeared in the striking band.

**3. Did not rewrite a title against dark impressions.** The one title this run
touched is a truncation fix, not a guess at query text. See below.

**4. No fresh `seo/gsc/` file.** `2026-09-01.json` is 6 days old, inside the 30
day rule. Nothing to compare.

**5. The warning count went 12 to 11 when #4 merged**, exactly as predicted, and
this run took it to 10. Nothing landed broken: `validate_site.py` reports no
problems and `audit_page_sources.py` reports 61 pages, 37 generated and 24 hand
written, with no page moving when the builders ran.

## What Search Console showed

The same file as the last four runs, so the numbers are unchanged and are not
repeated in full: 718 impressions and 2 clicks across 150 named queries for
2 to 29 August, against 2,938 impressions on the page dimension, of which 2,199
(75%) carry no query.

**The thing that matters this week is what the file cannot show.** All three
merged PRs shipped on 6 September. Every row in this file predates them by at
least eight days. The `card_velocity_exceeded` split, which three consecutive
runs have said PR #3's title rewrite is the cleanest test of, cannot be read at
all until a file exists whose window starts after 6 September. The same is true
of #4's title cut and #5's link. Three shipped changes are currently
unmeasurable, and will stay that way until the next fetch.

That converts the cadence question from a preference into a blocked measurement.
It is still Bruce's call, but it now has a cost attached.

## Changed

1. **`/blog/stripe-generic-decline-code/` and `/blog/do-not-honor-stripe-decline/`
   now link to each other.** This was the job the last log queued for the run
   after #4 merged. The two pages split the query `generic_decline`, 14
   impressions, at positions 42.5 and 43.5 with no link in either direction.
   The editorial case is stronger than the query case: the do_not_honor page
   carries a three row comparison table that names `generic_decline`, says in
   its own prose that it is "the one to separate out", mentions the code six
   times, and linked to its guide zero times. The generic_decline page did not
   mention `do_not_honor` at all. `do-not-honor-stripe-decline` is generated, so
   its link went into the `related` list in `build_content_pages.py`.
   `stripe-generic-decline-code` is one of the 24 hand written pages, so its
   card was added to `docs/blog/stripe-generic-decline-code/index.html` directly.

2. **`/blog/stripe-incorrect-cvc-decline/` title cut from 65 characters to 56.**
   "incorrect_cvc on Stripe: what it means and what actually fixes it" became
   "incorrect_cvc on Stripe: what it means and how to fix it". One of the 11
   truncating titles, and the one the routine's own criterion selects: the page
   sits in the striking band at position 7.2 with zero clicks. Its single named
   query, `incorrect cvc`, is one impression at position 11, so the other five
   impressions average about 6.4. This is not a rewrite aimed at query text we
   cannot see. The title truncates at any position, the defect is known
   independently of the query, and the shortened version drops only the word
   "actually" while keeping `incorrect_cvc`, Stripe, "what it means" and "fix".
   Hand written page, so all five metadata slots were moved together, plus the
   `h1`, which is where the blog hub reads its card heading and JSON-LD headline
   from. The hub picked both up on rebuild.

Both changes are published prose, so both went to a branch and a pull request
per Step 6. Nothing went to `main` this week.

Warning count is now 10.

## Considered and rejected

- **Acting on the `card_velocity_exceeded` split.** PR #3's title rewrite
  shipped 6 September and the only Search Console file available covers 2 to 29
  August. Touching that page again now would change two things before either
  could be read. Wait for a file whose window starts after 6 September.
- **A title rewrite on `/blog/stripe-subscription-past-due-vs-unpaid/`.**
  Unchanged from last run. 401 impressions, 93% dark, and the named rows rank
  6.8 and 8.2 against a page average of 18.5. Still guessing at 373 withheld
  searches. Do not re-propose until the page level export in "Needs Bruce" 4
  exists.
- **A title rewrite on `/blog/stripe-automatic-card-updates/`,
  `/blog/stripe-failed-payment-webhooks/` or
  `/blog/stripe-free-dunning-settings-checklist/`.** All three are 100% dark.
  The last is the tempting one, 10 impressions at position 5.5 with no clicks,
  the best average position on the site, but there is no query text at all to
  write toward. Same constraint, recorded for a third time.
- **The other nine truncating titles.** None has a query at a position where the
  click is in reach. The closest is `/blog/stripe-processing-error-decline/`,
  `processing_error` at 3 impressions and position 10.3 with a 63 character
  title. It is a fair candidate for a future week; it was left because taking
  two title changes in one run makes neither readable against the next file.
- **Hub links for the remaining four hard codes** (`incorrect_number`,
  `lost_card`/`stolen_card`, `pickup_card`, `authentication_required`).
  Unchanged from last run and still bulk without evidence. The comment in
  `build_content_pages.py` still records them.
- **The `the bank returned the decline code do_not_honor` split.** Genuinely
  mis-targeted: `/blog/stripe-currency-not-supported-decline/` takes 8 of 14
  impressions at position 81 while the actual do_not_honor page ranks 96. Every
  page in that split ranks between 65 and 96, so nothing there is close enough
  for a link or a title to convert. Noted, not acted on.
- **A new article.** No supporting query evidence, six runs running.
- **The commercial head terms.** `decline recovery` 82.1, `failed payment
  recovery` 95.0, `recover failed payments` 95.7. Unchanged and out of reach.
- **A fresh fetch.** Still a decision rather than work, and still Bruce's. See
  "Needs Bruce" 1.
- **The line ending noise in `git status`.** Declined a fourth time. Do not
  re-propose.

## Needs Bruce

1. **The cadence question is now blocking a measurement, not just a comparison.**
   Three prose changes shipped 6 September and no Search Console file can see any
   of them. The default 28 day fetch run on or after 29 September would cover
   roughly 1 to 28 September and would mostly postdate the merges, which is
   usable. `python3 scripts/fetch_gsc.py fetch --days 7` any time after
   13 September would give a cleaner read sooner. Either works; the current
   state, where nothing is scheduled, does not.
2. **The pull request from this run needs review.** One title cut and two links.
   The four run backlog cleared on 6 September, so this is the only outstanding
   one.
3. **`app.recoverflow.org` is still in the index**, 3 impressions on the brand
   query at position 10 against the marketing site's 7.1. Outside `docs/`, so
   this routine cannot reach it. Needs a `noindex` header or a robots rule on
   that deployment. Unchanged for three runs.
4. **Search Console's page level query export would unlock the biggest page on
   the site.** Filtering Performance to
   `/blog/stripe-subscription-past-due-vs-unpaid/` and exporting queries
   sometimes surfaces rows the site wide API call withholds. Its 373 dark
   impressions become writable if it works. `fetch_gsc.py import-csv` already
   accepts the export. Unchanged from last run.

## Next run, in order

1. Whether this run's pull request merged, and whether a fresh `seo/gsc/` file
   exists. If one does and its window starts after 6 September, the first job is
   reading whether PR #3 closed the `card_velocity_exceeded` split and whether
   PR #5's link moved `transaction_not_allowed`. Those are the first outcomes
   this routine will be able to measure.
2. Pull before Step 1. This run's first health check ran against a checkout three
   commits behind and reported a warning count that had already been fixed.
3. Run `python3 scripts/report_gsc.py` first, as always. Page dimension is the
   first table.
4. Do not rewrite a title against dark impressions. The constraint holds unless
   the page level export in "Needs Bruce" 4 has been run.
5. Warning count is 10. If it rises, a page shipped with a title that will not
   fit. `/blog/stripe-processing-error-decline/` is the next fair candidate to
   take it to 9, if a query supports it.
