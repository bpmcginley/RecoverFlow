# 2026-08-31

First run against `seo/ROUTINE.md`. `seo/log/` was empty, so nothing here is a
repeat and nothing was inherited as already rejected.

## Health check

`validate_site.py` passed on a clean checkout: 61 pages, no problems. Ran the
full build chain for all four builders plus `apply_design_system.py` and
confirmed it still passes afterwards.

## What Search Console showed

One file exists, `seo/gsc/2026-08-30.json`, covering 31 July to 27 August 2026.
There is no second file, so no page could be compared against its own previous
position. That comparison starts being possible next week.

754 impressions and 2 clicks across 183 queries and 53 pages.

Queries at position 5 to 20 with impressions and no clicks, which is where the
routine says the return is:

| Query | Impressions | Position | Page |
| --- | --- | --- | --- |
| `ai10325` | 94 | 4.2 | `/blog/visa-excessive-reattempts-rule/` |
| card velocity exceeded meaning | 42 | 8.3 | `/blog/stripe-card-velocity-exceeded/` |
| card velocity exceeded | 16 | 12.4 | same |
| what does card velocity exceeded mean | 15 | 9.3 | same |
| `payment_intent.payment_failed` | 6 | 11.7 | `/blog/invoice-payment-failed-vs-payment-intent-payment-failed/` |
| stripe past due (and four variants) | 13 | 6.8 to 9.5 | `/blog/stripe-subscription-past-due-vs-unpaid/` |

Everything else with real impressions ranks between 40 and 96:
`/tools/decline-code-lookup/` at 101 impressions, `/recover-failed-stripe-payments/`
at 95, `/blog/do-not-honor-stripe-decline/` at 93, `/blog/how-stripe-smart-retries-work/`
at 67. A title rewrite does not move a page sitting at position 90.

No query with meaningful impressions lacks a page. Every head query already maps
to something we have built, so there is no article candidate in this data.

## Changed

1. `build_sitemap.py` now dates each URL from the last commit that touched its
   `index.html` rather than from the build date. It was stamping today on all 61
   URLs on every run, which tells search engines the whole site changed each time
   and is the reliable way to get `lastmod` discounted. Committed to `main`.
   The rebuild in this run moved exactly the two pages whose HTML actually
   changed, which is the check that it works.
2. `docs/llms.txt` gained `/tools/` and `/audit/`, the only two of the 61 built
   pages it was missing. Committed to `main`.
3. `/blog/visa-excessive-reattempts-rule/` title and meta description now name
   Visa bulletin AI10325 and its 17 April 2021 effective date. On a branch,
   see below.

## Considered and rejected

- **`/blog/stripe-card-velocity-exceeded/` title and description.** This is the
  best opportunity on the site, 113 impressions and no clicks with the page
  sitting at 8.3 for "card velocity exceeded meaning", and the queries are
  worded "meaning" and "what does it mean" while the title is not. Not done: no
  script emits that page. Blocked on the finding below, not on the merits.
- **`/tools/decline-code-lookup/`, `/recover-failed-stripe-payments/`,
  `/blog/do-not-honor-stripe-decline/`, `/blog/how-stripe-smart-retries-work/`.**
  Between them the four largest impression counts after the velocity cluster,
  and all four rank between 40 and 96. Metadata is not the constraint at that
  depth. Do not re-propose a title rewrite for these on impression count alone.
- **`/blog/stripe-subscription-past-due-vs-unpaid/`.** Ranks 6.8 to 9.5 on the
  `past_due` variants and earned one of the site's two clicks. Its title already
  contains `past_due`, `unpaid` and `canceled` verbatim. 29 impressions total, so
  the ceiling on any rewrite is small, and the current title is not the problem.
- **A new article.** No supporting query evidence, see above.
- **Adding AI10325 to the visa page's `answer` block.** Held back to keep this
  week's change to the cheapest thing that could work. Worth doing if the title
  change moves impressions but not clicks.

## Needs Bruce

**1. Twenty-four of the 61 pages in `docs/` have no generator.** The routine
opens by saying every file in `docs/` is generated and that the fix for any page
is to edit the script that emits it. That is not true of these, and it is the
reason the biggest win this week could not be taken. Verified by snapshotting
mtimes, running all four builders, and listing what was never written:

    docs/index.html
    docs/pricing/index.html
    docs/recover-failed-stripe-payments/index.html
    docs/compare/stripe-native/index.html
    docs/tools/index.html
    docs/tools/decline-code-lookup/index.html
    docs/tools/dunning-email-generator/index.html
    docs/tools/recovery-estimator/index.html
    docs/tools/retry-schedule-builder/index.html
    docs/tools/visa-retry-budget/index.html
    docs/blog/invoice-payment-failed-vs-payment-intent-payment-failed/index.html
    docs/blog/stripe-call-issuer-decline/index.html
    docs/blog/stripe-card-velocity-exceeded/index.html
    docs/blog/stripe-currency-not-supported-decline/index.html
    docs/blog/stripe-duplicate-transaction-decline/index.html
    docs/blog/stripe-fraudulent-decline-code/index.html
    docs/blog/stripe-generic-decline-code/index.html
    docs/blog/stripe-incorrect-cvc-decline/index.html
    docs/blog/stripe-incorrect-number-vs-invalid-number/index.html
    docs/blog/stripe-lost-card-stolen-card-declines/index.html
    docs/blog/stripe-pickup-card-vs-restricted-card/index.html
    docs/blog/stripe-processing-error-decline/index.html
    docs/blog/stripe-transaction-not-allowed-decline/index.html
    docs/blog/stripe-try-again-later-decline/index.html

The six decline guides came in hand-written at 88c7cea on 12 August 2026 and the
rest predate it. `apply_design_system.py` and `validate_site.py` both cover them,
so they are not broken, they are just unreachable from the routine. Two ways out:
port them into `ARTICLES` and the other builders so the routine can work on them,
or write down in `ROUTINE.md` that these files are hand-maintained and may be
edited directly. Either is fine. Guessing is not, so nothing was touched.

**2. `/tools/` says both four and six.** Its meta description reads "Four free
browser tools" while the page body says "Six free tools" and links six. The body
is right. It is one of the pages above, so it was left alone.

**3. The pull request needs opening by hand.** `gh` is not installed on this
machine, on the Git Bash path or the Windows path. The work is committed and
pushed to `seo/2026-08-31-visa-ai10325`:

    https://github.com/bpmcginley/RecoverFlow/pull/new/seo/2026-08-31-visa-ai10325

One commit, `abf012e`, four files, plus 16 minus 11 lines. It changes the title
and description of one article and nothing else. Per Step 6 it was not committed
to `main`.

## Next run, in order

1. Whether AI10325 converted. `/blog/visa-excessive-reattempts-rule/` had 94
   impressions at position 4.2 and zero clicks in this window. Pull a fresh
   `seo/gsc/` file and compare that one row against `2026-08-30.json`. It is the
   cleanest before-and-after the site is going to get, and it also says whether
   the same treatment is worth applying elsewhere.
2. Whether the orphan-page question above was answered. If it was, the velocity
   cluster is the next change and it is worth more than anything else available.
3. Whether the pull request merged. If it did not, do not redo the work.
