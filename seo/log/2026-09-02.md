# 2026-09-02

Third run against `seo/ROUTINE.md`. The previous log is `2026-09-01.md` and its
"Next run, in order" list is answered first.

This run made no change to any page. Nothing on the site is broken, both prose
levers are held behind unmerged pull requests, and the Search Console file is one
day old and covers the same days the last run already read. The work went into
the one thing that was genuinely missing, which was any measurement of Step 2.

## Answering last week's four questions

**1. Neither PR #3 nor PR #4 has merged.** Both are still open. No work was
redone and no file either one touches was edited. Between them they cover
`scripts/build_content_pages.py`, `docs/sitemap.xml`, `docs/blog/index.html` and
five article pages. Because both touch `build_content_pages.py`, every generated
article on the site was off limits this run: a title, a description, a `related`
link or a new article all land in that file and would have conflicted with both.

**2. The velocity link into `/blog/visa-excessive-reattempts-rule/` was not
taken**, for the same reason as last week. PR #3 still edits that article's dict.

**3. No fresh `seo/gsc/` file was pulled.** `2026-09-01.json` is one day old.
Last week's instruction was to pull only if the cadence question is settled or
four weeks have passed, and neither is true.

**4. The warning count is still 12** and the same 12 pages. `validate_site.py`
reports no problems and `audit_page_sources.py` reports the split unchanged at
61 pages, 37 generated and 24 hand-written. Nothing landed broken.

Two checks the validator does not make were run by hand and both came back
clean: every internal link in all 61 pages resolves to a page that exists, and
every canonical matches its own URL.

## What Search Console showed

Same file the last run read, so the striking distance table has not changed and
is not repeated here. 718 impressions and 2 clicks, 2 to 29 August.

One thing in it had not been read before, and it was visible in both existing
files. **On exact decline code queries the site returns two pages, the guide and
`/tools/decline-code-lookup/`, and the lookup page is the one Google shows more
often while ranking far worse.**

| Query | Guide | Lookup tool |
| --- | --- | --- |
| `card_velocity_exceeded` | 13 impr at 19.5 | 15 impr at 43.4 |
| `card_velocity_exceeded.` | 7 impr at 25.9 | 14 impr at 43.4 |
| `transaction_not_allowed` | 5 impr at 21.4 | 10 impr at 42.1 |
| `do_not_honor decline code` | 1 impr at 42.0 | 6 impr at 84.2 |
| `the bank returned the decline code do_not_honor` | 1 impr at 65.0 | 4 impr at 76.0 |

Six queries are flagged this way in total and the lookup tool is the weaker page
in five of them. It is worth knowing that the natural language forms of the same
question do not split at all: "card velocity exceeded meaning" is 42 impressions
at 8.3 on the guide alone. The split is specific to the bare code string.

**No change was made about it, deliberately.** The diagnosis does not support one
yet. See below.

## Changed

1. **`scripts/report_gsc.py`, new.** Reads the newest `seo/gsc/*.json` and prints
   Step 2. Read only, never writes, never fails a build. It prints the striking
   distance table, the split above, the queries where nothing of ours ranks, and
   the comparison against the previous file. Committed to `main`.
2. **The comparison in that script refuses to run when it would be noise.** It
   measures how many days the two files share and prints the overlap rather than
   a delta when it is above 50%. On today's two files that is 26 of 28 days, 93%,
   which is the number last week's log worked out by hand. This does not answer
   the cadence question in "Needs Bruce" below, it just stops the routine
   producing a fake answer to it while the question is open. Committed to `main`.
3. **`ROUTINE.md` Step 2 now runs the script**, and gained a fourth thing to pull
   out of the data, which is the split. The "pages that lost position" bullet
   now says the comparison is gated on overlap. Committed to `main`.

All three are maintenance and went to `main`. No pull request was opened this
run because no published prose changed.

## Considered and rejected

- **Acting on the decline code split.** The obvious remedy is not available,
  because the page is not defective. `/tools/decline-code-lookup/` already links
  to all 14 per-code guides, carries one card per code with a one line meaning
  and a next step, and mentions each code exactly once. It is a real index, not a
  duplicate of the guides, so there is no thin or copied content to remove. What
  is left is index versus detail ambiguity on a bare code string, and the honest
  next move is to wait: PR #3 rewrites the velocity guide's title to
  `Card velocity exceeded: what the Stripe decline code means`, which is exactly
  the kind of change that would resolve the split on its own. Acting now would
  make it impossible to tell which change did what, which is the mistake last
  week's log warned against.
- **Anything touching a generated article.** Both open PRs edit
  `build_content_pages.py`. A one line `desc` change would have conflicted with
  both.
- **The 12 long titles.** Unchanged from last week and the reasoning holds. The
  only one in striking range is `/blog/stripe-incorrect-cvc-decline/` at 7.2 on
  6 impressions. `/blog/invoice-payment-failed-vs-payment-intent-payment-failed/`
  is already being cut in PR #4, so the count drops to 11 when that merges.
- **The 37 files `git status` reports as modified on a clean checkout.** Last
  week recorded this as mtime and not content, and that is not quite the reason:
  `core.autocrlf` is `true` and the builders write LF, so the working copy
  differs from the checkout in line endings only. `git diff --numstat` is empty
  either way. A `.gitattributes` rule would silence it. Not worth 37 rewritten
  files to fix a cosmetic status line, and last week already declined it. Do not
  re-propose without a reason beyond tidiness.
- **A new article.** No supporting query evidence, three runs running.
- **The commercial head terms.** `decline recovery` 82.1, `failed payment
  recovery` 95.0, `recover failed payments` 95.7, `subscription payment
  recovery` 91.8. Unchanged and out of reach.

## Needs Bruce

1. **PR #3 has been open since 31 August, PR #4 since 1 September.** This is now
   the binding constraint on the routine rather than a note in passing. Two
   consecutive runs have been unable to touch any generated article because both
   PRs edit `build_content_pages.py`, and a third will be in the same position.
   The queue is the work, not the writing.
2. **The cadence question from last week is still open** and is unchanged: a 28
   day fetch on a weekly cycle overlaps by 26 days. The new script now makes the
   limitation visible instead of silently inviting a bogus delta, but it cannot
   pick between running monthly, shortening the window, or comparing only files
   four weeks apart.
3. **`app.recoverflow.org` is still in the index**, 3 impressions on the brand
   query at position 10 against the marketing site's 7.1. Outside `docs/`, so
   this routine cannot reach it. It needs a `noindex` header or a robots rule on
   that deployment.

## Next run, in order

1. Whether PR #3 and PR #4 merged. If they did, the first job is the velocity
   link into `/blog/visa-excessive-reattempts-rule/` that two runs have now
   wanted and could not take.
2. Run `python3 scripts/report_gsc.py` before reading anything else.
3. If PR #3 merged, check whether the velocity guide's new title closed the
   `card_velocity_exceeded` split. That is a real hypothesis with a real test
   and it is the most interesting question the data currently holds.
4. A fresh `seo/gsc/` file is due on or after 2026-09-29, which is four weeks
   after `2026-09-01.json`, unless the cadence question is settled sooner.
5. The warning count is 12. It should become 11 when PR #4 merges. If it goes
   up, a page shipped with a title that will not fit.
