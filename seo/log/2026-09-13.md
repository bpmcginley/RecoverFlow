# 2026-09-13

Eleventh run against `seo/ROUTINE.md`. Previous log is `2026-09-11.md`. Started as the
scheduled run (Steps 0-2 only, nothing had changed, no log written at that point), then
Bruce asked directly to clear the four "Needs Bruce" items from the last three logs. This
log covers that second half.

## What changed

1. **Schedule fixed.** `recoverflow-seo-optimization-routine` was `0 10 * * *` against a
   file headed "Weekly SEO cycle." Changed to `0 10 * * 1`, Mondays only. This is the
   Claude Code scheduler config, not a file in this repo, so there is nothing to commit for
   it. Next automated run: 2026-09-14 (still one day before the GSC window below goes
   clean, so it should find the same "not yet" state Steps 0-2 already confirmed today).

2. **PR #6 and PR #8 merged.** Both were straightforward, matching the routine's own
   established pattern: a title truncation cut each, a reciprocal internal link each,
   sitemap and (for #8) `llms.txt` regenerated. No results claims, no em dashes, no new
   unsourced factual claims, CI green on both after updating their branches. They touched
   overlapping generated files (`docs/blog/index.html`, `docs/sitemap.xml`), so merging #6
   first put #8 in conflict; resolved by merging `main` into #8's branch, taking `main`'s
   copy of the disputed generated file, and **rebuilding rather than hand-resolving**:
   `build_content_pages.py` -> `apply_design_system.py` -> `build_sitemap.py`, then
   `validate_site.py` and `audit_page_sources.py` both clean before pushing. `git diff
   --stat` after the rebuild showed real diffs in exactly the two generated files touched
   (`docs/sitemap.xml` from lastmod dates, the merge itself for the rest), nothing wider.
   Title-length warning count: 11 to 9. The five-item prose queue every log since
   2026-09-06 has been counting down against is now fully merged.

3. **Page-level query export done directly, no manual Search Console trip needed.** Item 4
   asked for a page-filtered export for `/blog/stripe-subscription-past-due-vs-unpaid/`,
   the 401-impression page three consecutive logs called dark. The service account key
   already in `~/.recoverflow/gsc-key.json` can do this without a UI export:
   `fetch_gsc.py`'s own `query()` function takes a `dimensionFilterGroups` argument the CLI
   just never exposed. Ran a one-off script reusing `fetch_gsc.access_token()`, filtered to
   that one page, over the same window as `seo/gsc/2026-09-01.json` (2-29 August) so it
   reads against the same baseline. Result, committed as
   `seo/gsc/page-stripe-subscription-past-due-vs-unpaid-2026-08-02_2026-08-29.json`:
   **28 of 401 impressions (7%) carry query text**, confirming the 93% dark figure already
   on record rather than changing it. The 11 named rows: `past_due` (10 impr, position
   21.3), `stripe past due` (4, position 6.75), `past_due stripe` (4, position 8.25),
   `stripe past_due` (2, position 9.5), `stripe unpaid subscription` (2, position 8.5), and
   six one-impression long-tail rows including two invoice-cancellation queries and one
   verbatim Stripe error string. Filed as data, not acted on: a title rewrite off this is a
   new prose decision under Step 3, not the fetch this run was asked to make possible, and
   the bare term `past_due` ranking 21.3 while its close variants with "stripe" attached
   rank 6.75-9.5 is a real candidate for the next run to weigh, not something to ship
   inside a data-fetch task.

   Confirmed while testing the filter: querying the same page over a **current** 28 day
   window (14 August-10 September) returns 219 impressions on the page dimension and
   **zero** named-query rows. Every query on this page in the most recent four weeks is
   individually below whatever floor Search Console reports at, worse than the older
   window's 7%. Worth knowing before reading this page's numbers as improving just because
   impressions look steady.

## What was flagged rather than run

**Item 3, `fetch_gsc.py fetch --days 7`, was not run.** Three consecutive logs
(2026-09-09, -10, -11) worked out why: with `end = today - lag(3)` and `start = end -
6`, today's date has to be 2026-09-16 or later before a 7 day fetch stops straddling the
6 September merges. Today is 2026-09-13. Running it now would give `start = 2026-09-04`,
still three days on the wrong side of the merges, the exact muddled window those three
logs already rejected for the exact same reason. Running it anyway is a three-day wait
away from being clean, and the resulting file would become "the newest `seo/gsc/*.json`"
that the next automated run reads to decide whether the prose-queue count restarts and
whether the `card_velocity_exceeded` split is readable, so a muddled file here has a real
cost beyond this run. Left this one for 2026-09-16 rather than doing it under the "do all
four" instruction, since doing it now would contradict the reasoning behind the request
that flagged it in the first place. Flagging back to Bruce rather than silently deciding
either way.

## Considered and rejected

- **Writing a title rewrite for `/blog/stripe-subscription-past-due-vs-unpaid/` off the new
  page-level data.** The prose queue just cleared to zero, but this would be a fresh
  Step 3 decision, not part of what was asked, and the page's 93% dark share means seven
  named queries are a thin basis for a rewrite regardless.
- **Backdating this log's window to before the PR merges.** The merges happened inside this
  same run, so there is no clean "before" state to compare against; recorded as one run.

## Needs Bruce

1. **Confirm whether to run the 7 day fetch now anyway, muddled window and all, or wait
   for 2026-09-16.** Recommendation is to wait three days; the next automated run
   (2026-09-14, Monday) will re-confirm the window is still dirty and cost nothing by
   waiting.
2. **`app.recoverflow.org` is still indexed**, unchanged from the last three logs. Outside
   `docs/`, so this routine cannot reach it.

## Next run, in order

1. If today is 2026-09-16 or later and no fresher file exists yet, run
   `python3 scripts/fetch_gsc.py fetch --days 7`.
2. Once that file lands, read the `card_velocity_exceeded` split first, then re-open
   whether `past_due` on `/blog/stripe-subscription-past-due-vs-unpaid/` is worth a title
   change now that its position (21.3) is known.
3. The title-length backlog is down to 9. Same rule as always: take one only when a query
   says the page is close enough for the click to matter.
