## .NET
bin/
obj/
*.user
*.suo

## Secrets — never commit real Stripe/SendGrid keys or the encryption key
appsettings.Development.json
appsettings.Local.json
appsettings.Production.json
*.pfx

## Logs
*.log

## Skills lockfile symlink noise (safe to keep, but ignore local cache if regenerated)
.agents/.cache/

## Editor/office lock files
~$*

## OS
.DS_Store
Thumbs.db
__pycache__/

# Local planning notes. Public repo, keep GTM material out of it.
PLAN.md
AUDIT_REPLIES.md
LINKEDIN_LEADGEN.md
COMPETITIVE.md
STRIPE_APP_PLAN.md
VALIDATION.md

# Same rule, and this half was missed: the ledger carries real people's names and
# work emails, and none of them agreed to appear on a public GitHub repo.
growth/pipeline.csv
growth/sequences/
growth/sender.txt

# Channel research and drafted posts. Same reason as COMPETITIVE.md: it reads as
# a playbook for competing with RecoverFlow, and it is written to be read by us.
growth/channels.md
growth/reddit.md
growth/directory-listings.md
# Same again: partner research naming real firms, their staff certifications and the
# hooks we would use on them. Fine as private notes, not fine as a public playbook.
growth/partners.md

# Stripe app build output. Regenerated by `stripe apps upload`.
stripe-app/.build/
stripe-app/node_modules/
